# create workflow file
name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write
# add a job to workflow file
jobs:
  welcome:
    name: Post welcome comment
    runs-on: ubuntu-latest
